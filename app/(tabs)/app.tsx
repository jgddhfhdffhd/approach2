import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import AppNavigator from '../../navigation/AppNavigator';  // 你的 AppNavigator 路径

const App: React.FC = () => {
  return (
   
      <AppNavigator />
    
  );
};

export default App;
